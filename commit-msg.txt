🐛 修復答題人數顯示和頭像顯示問題

🔧 問題修復：
1. **人數顯示錯誤**：
   - 改進 fetchAnswerDistribution 數據處理邏輯
   - 添加詳細的調試日誌
   - 確保用戶數據正確映射和計算

2. **頭像沒有顯示**：
   - 簡化頭像顯示條件邏輯
   - 移除過於嚴格的條件檢查
   - 添加「暫無人選擇」的提示

📊 數據處理優化：
- 重構答題分佈計算邏輯
- 改善用戶數據提取和處理
- 添加預設值處理未知用戶情況
- 增強錯誤處理和數據驗證

🎨 界面改進：
1. **條件渲染優化**：
   ```tsx
   // Before: 過於嚴格的條件
   {timeLeft <= 0 && distribution && distribution.users && distribution.users.length > 0 && (...)}
   
   // After: 簡化條件，內部處理
   {timeLeft <= 0 && (
     <div>
       {distribution && distribution.users && distribution.users.length > 0 ? (
         // 顯示頭像
       ) : (
         // 顯示「暫無人選擇」
       )}
     </div>
   )}
   ```

2. **空狀態處理**：
   - 沒有人選擇時顯示「暫無人選擇」
   - 統計數據仍然顯示「0 人 (0%)」
   - 保持界面一致性

🔍 調試功能增強：
- 添加原始數據日誌記錄
- 詳細的分佈計算日誌
- 用戶數據結構驗證日誌
- 便於問題診斷和追蹤

💡 技術改進：
- 更健壯的數據處理邏輯
- 更好的錯誤處理機制
- 更清晰的條件判斷邏輯
- 更詳細的調試信息

🎯 用戶體驗：
- ✅ 正確顯示每個選項的答題人數
- ✅ 準確計算和顯示百分比
- ✅ 正常顯示玩家頭像和姓名
- ✅ 空狀態有適當的提示信息
- ✅ 統計數據實時更新
